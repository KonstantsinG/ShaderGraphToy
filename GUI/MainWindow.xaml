<Window x:Class="GUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:glWpfControl="clr-namespace:OpenTK.Wpf;assembly=GLWpfControl"
        xmlns:controls="clr-namespace:GUI.Controls"
        xmlns:comps="clr-namespace:GUI.Components"
        mc:Ignorable="d"
        Title="MainWindow" Height="800" Width="1300">

    <Grid Background="#FF27282D">

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="58*"/>
            <ColumnDefinition Width="42*"/>
        </Grid.ColumnDefinitions>

        <Grid>

            <Canvas x:Name="MainCanvas" Width="Auto" Height="Auto"
                    Background="{StaticResource Gray_01}" AllowDrop="True">

                <controls:GraphNode  Canvas.Left="100" Canvas.Top="100"
                                     Width="150" Height="200"/>

                <controls:GraphNodeBase Canvas.Left="400" Canvas.Top="400"
                                        Width="150" Height="200"
                                        HeaderColor="#FF1D1C17"/>

            </Canvas>

        </Grid>

        <ContentPresenter Grid.Column="1">
            <ContentPresenter.Content>

                <comps:RenderingViewport/>

            </ContentPresenter.Content>
        </ContentPresenter>

    </Grid>
    
</Window>
